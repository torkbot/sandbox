[package]
name = "sandbox"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
krun-devices = { path = "../../deps/libkrun/src/devices", default-features = false, features = ["blk"] }
krun = { package = "libkrun", path = "../../deps/libkrun/src/libkrun", default-features = false, features = ["blk"] }
libc = "0.2"

[lib]
crate-type = ["rlib"]
