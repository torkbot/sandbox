[package]
name = "sandbox"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
krun-devices = { path = "../../deps/libkrun/src/devices", default-features = false }
krun = { package = "libkrun", path = "../../deps/libkrun/src/libkrun", default-features = false }
libc = "0.2"

[lib]
crate-type = ["rlib"]
